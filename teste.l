/* Exemplo de Programa na linguagem L */

int n<--5, a<- -3.55 , c<- -0x55;
string nome;
const MAXITER= -110.5555;

write("Digite seu nome: ");
readln(nome);

float percentual;
while n<=MAXITER {
  percentual<-float(n)/MAXITER;
  writeln(percentual,": Ola' ",nome);
  n<- 3< n+1+(float(int(5))) * (5 + -id[5*3]);
  ;
}
